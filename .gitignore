__pycache__/
*.pyc
.venv/
.pytest_cache/
*.egg-info/

# data and artifacts — never committed
data/
checkpoints/
# bundled portable binaries (fluidsynth), downloaded per-machine
tools/
*.bin
*.wav
*.sf2
# exported model build artifact — hosted on HF, regenerate via export_onnx.py
*.onnx
